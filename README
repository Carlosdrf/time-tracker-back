In order to run the app you must run 

```bash
docker compose up
```

then, import the database into the `inimble_db` container

to start the app in local environment for development run
# npm run dev
